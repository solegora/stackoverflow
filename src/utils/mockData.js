// Mock data for the followUser function
export const followUser = (userId) => {
  // Simulate a successful follow action
  console.log(`User ${userId} followed`);
};

// Mock data for the blockUser function
export const blockUser = (userId) => {
  // Simulate a successful block action
  console.log(`User ${userId} blocked`);
};
